import { IUserResponse } from 'src/user/schemas/user-response.interface';
import { IBook } from '../interfaces/book.interface';

export const reserveBookHelper = async (book: IBook, user: IUserResponse) => {};
